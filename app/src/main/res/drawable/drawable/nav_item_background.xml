<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused or Hover State -->
    <item android:state_focused="true" android:drawable="@color/hover_blue" />
    <!-- Pressed State -->
    <item android:state_pressed="true" android:drawable="@color/hover_blue" />
    <!-- Default State -->
    <item android:drawable="@color/transparent" />
</selector>
